spring:
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      hikari:
          jdbc-url: jdbc:mysql://125.129.74.131:3306/querydsl?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
          username: randy
          password: ghtjq2959@
  jpa:
      hibernate:
        ddl-auto: create # ?? ??? ?? ??? drop ? ?? ??

      properties:
          hibernate:
              format_sql: true
#              show_sql: true
#              use_sql_comment: true
      database-platform: org.hibernate.dialect.MySQL5Dialect
logging:
    level:
        org.hibernate.SQL: debug
        org.hibernate.type: trace
