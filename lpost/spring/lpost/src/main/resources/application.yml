spring:
  application:
    name: lpost
  datasource:
    url: jdbc:h2:mem:post
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect