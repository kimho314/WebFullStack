version: "3.1"
services:
  db:
    image: postgres:latest
    container_name: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: "luna"
      POSTGRES_PASSWORD: "0314"
      POSTGRES_HOST_AUTH_METHOD: md5